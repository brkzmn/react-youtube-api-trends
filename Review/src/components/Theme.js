import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    mode: "dark",
    primary: {
      main: "#fafafa",
    },
    secondary: {
      main: "#f44336",
    },
    text: {
      primary: "#fafafa",
      secondary: "#fafafa",
    },
  },
});
