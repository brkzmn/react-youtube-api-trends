import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    mode: "dark",
    primary: {
      // main: "#ED3833",
      main: "#fafafa",
    },
    secondary: {
      // main: "#f50057",
      main: "#f44336",
    },
    text: {
      primary: "#fafafa",
      secondary: "#fafafa",
      // secondary: "#ED3833",
    },
  },
});
